{% block real_ip %}
    {% if (real_ip_trusted_sources | default([])) %}
        real_ip_header {{ real_ip_header | default('X-Real-IP') }};

        {% for ts in real_ip_trusted_sources %}
            set_real_ip_from {{ ts }};
        {% endfor %}
    {% endif %}
{% endblock %}
